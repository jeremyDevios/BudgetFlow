services:
  app:
    build: .
    container_name: budgetflow-app
    restart: always
    ports:
      - "8095:8095"
    environment:
      - NODE_ENV=production
      - PORT=8095
    env_file:
      - .env.local
    volumes:
      - .:/app
      - /app/node_modules